[package]
name = "learning_mode_windows"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Windows learning-mode backend for MXC: captures AppInfo-brokered ETW denial traces via processmodel.dll and decodes them into cross-platform records."

[dependencies]
thiserror = { workspace = true }

[target.'cfg(target_os = "windows")'.dependencies]
wxc_common = { workspace = true }
learning_mode_core = { workspace = true }
windows = { workspace = true }
windows-core = { workspace = true }

[target.'cfg(target_os = "windows")'.dev-dependencies]
sandbox_spec = { workspace = true }
flatbuffers = { workspace = true }
